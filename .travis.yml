language: rust

rust:
  - 1.0.0-beta.2
  - nightly

install:
  - sudo apt-get update -qq
  - sudo apt-get install libassimp-dev

before_script:
  - rustc -V
  - cargo -V

script:
  - cargo build -v
  - cargo test -v
